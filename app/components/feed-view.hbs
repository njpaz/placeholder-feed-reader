<div class="accordion">
  {{#each @feedItems as |item index|}}
    <FeedView::Item
      @item={{item}}
      @index={{index}}
      @selectedIndex={{this.selectedIndex}}
      @selectVisibleIndex={{fn this.selectVisibleIndex index}} />
  {{/each}}
</div>
